# Core
pandas>=1.5
pyodbc>=4.0

# Service layer
fastapi>=0.110
pydantic>=2.0
uvicorn>=0.27

# LLM provider (only the one you use)
anthropic>=0.40
# openai>=1.30

# Optional production add-ons:
# redis>=5.0            # conversation memory at scale
# pinecone-client>=3.0  # or faiss-cpu / pgvector for RAG retrieval
# PyJWT>=2.8            # bearer-token verification
